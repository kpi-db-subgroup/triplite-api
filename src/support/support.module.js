'use strict';

const SupportEntityBuilder = require('./support.entity-builder');
const SupportRepository = require('./support.repository');
const SupportService = require('./support.service');
const SupportController = require('./support.controller');

module.exports = {
  SupportEntityBuilder,
  SupportRepository,
  SupportService,
  SupportController,
};
